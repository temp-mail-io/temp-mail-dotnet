name: Pushed to `feature/**`

on:
  push: 
    branches:
      - "feature/**"
        
        
jobs: 
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - name: Run Whole Pipeline
        uses: ./.github/dotnet-release.yml
        with:
          pre_release: true